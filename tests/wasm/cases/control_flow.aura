// Test: Control Flow (if/else)
// Expected output:
// Large
// Medium
// Small

function classify(count: i32): i32 {
  if (count > 10) {
    print "Large";
  } else if (count > 0) {
    print "Medium";
  } else {
    print "Small";
  }
  return 0;
}

classify(20);
classify(5);
classify(-1);
